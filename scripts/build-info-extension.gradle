import org.apache.tools.ant.filters.ReplaceTokens

processResources {
    filter(ReplaceTokens, tokens: [
            NAME   : project.name,
            VERSION: project.version,
            GIT    : '-'
    ])
}
